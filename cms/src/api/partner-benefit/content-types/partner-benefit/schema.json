{
  "kind": "collectionType",
  "collectionName": "partner_benefits",
  "info": {
    "singularName": "partner-benefit",
    "pluralName": "partner-benefits",
    "displayName": "Partner Benefit",
    "description": "A benefit of partnering with Beacon, shown on the partners page."
  },
  "options": { "draftAndPublish": true },
  "pluginOptions": {},
  "attributes": {
    "title": { "type": "string", "required": true },
    "description": { "type": "text" },
    "icon": { "type": "media", "multiple": false, "allowedTypes": ["images"] },
    "order": { "type": "integer", "default": 0 },
    "region": {
      "type": "enumeration",
      "enum": ["global", "ae", "sa"],
      "required": true,
      "default": "global"
    }
  }
}
